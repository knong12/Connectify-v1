export default function ProfilePage() {
  return (
    <section className="card">
      <h2>Profile</h2>
      <p>
        This page will show the signed-in user, top genres, top artists, and
        top tracks after the Spotify callback and sync endpoints are wired up.
      </p>
    </section>
  );
}

